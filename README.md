# Jorge Tech – Proyecto Integrador con Flask

Rutas implementadas: `/`, `/productos`, `/clientes`, `/proveedores`, `/facturacion`.

## Ejecutar

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Abrir: `http://127.0.0.1:5000`
